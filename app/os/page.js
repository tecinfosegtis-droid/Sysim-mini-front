'use client';
import { useEffect, useState } from 'react';
import { supabase } from '../lib/supabaseClient';

export default function OSPage() {
  // Coloque aqui depois o seu código de OS.
  return <div>Ordens de Serviço</div>;
}
