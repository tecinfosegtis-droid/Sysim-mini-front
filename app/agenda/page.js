'use client';
import { useEffect, useState } from 'react';
import { supabase } from '../lib/supabaseClient';

export default function AgendaPage() {
  // Coloque aqui depois o seu código da Agenda (estados, useEffect, JSX).
  return <div>Agenda</div>;
}
